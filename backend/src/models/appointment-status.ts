/**
 * Represents appointment status.
 *
 * Statuses:
 * - `PENDING`
 * - `COMPLETED`
 * - `CANCELLED`
 * - `MISSED`
 */
export enum AppointmentStatus {
   PENDING = 'pending',
   COMPLETED = 'completed',
   CANCELLED = 'cancelled',
   MISSED = 'missed',
}
