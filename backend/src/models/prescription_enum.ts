/**
 * Represents prescription status.
 *
 * Statuses:
 * - `PENDING`
 * - `APPROVED`
 * - `CANCELLED`
 * - `DENIED`
 */
export enum PStatus {
   PENDING = 'Pending',
   APPROVED = 'Approved',
   DENIED = 'Denied',
   CANCELED = 'Canceled',
}
