/**
 * Represents prescription status.
 *
 * Statuses:
 * - `PENDING`
 * - `APPROVED`
 * - `CANCELLED`
 * - `DENIED`
 */
export enum PrescriptionStatus {
   PENDING = 'Pending',
   APPROVED = 'Approved',
   DENIED = 'Denied',
   CANCELED = 'Canceled',
}
